spring:
  profiles:
    active: test

http:
  proxyHost: localhost
  proxyPort: 8080
  no-proxy: localhost
https:
  proxyHost: localhost
  proxyPort: 8080

nav.no:
  authorization:
    api-gw-header: x-nav-apiKey
    api-gw-key: mock-key
  gateways:
    k9_lookup_url: 'http://localhost:8080/child-lookup'
  cors-addresses: 'http://localhost:**'